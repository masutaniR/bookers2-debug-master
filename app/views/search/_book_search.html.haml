%h2
  Books search for '#{value}'
%table.table
  %thead
    %tr
      %th
      %th Title
      %th Opinion
  - datas.each do |book|
    %tbody
      %tr
        %th
          = attachment_image_tag(book.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg", size:'40x40')
        %th
          = book.title
        %th
          = book.body
